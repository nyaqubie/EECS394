The local.properties file must have the following entries to work properly: path to your sdk, path to a browser, and your os (windows or linux).

This will select the proper test suite to launch.

sdk.dir = C:\\android-sdk
browser = C:/Program Files (x86)/Mozilla Firefox/firefox.exe
os = windows